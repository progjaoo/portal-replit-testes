## Packages
date-fns | Formatting dates in tables and forms

## Notes
- Using Shadcn UI Sidebar pattern for the main layout.
- Editoriais and TemaEditoriais currently only support List and Create based on the provided API contract.
- Posts and Emissoras support full CRUD.
- Native `<input type="color">` used for TemaEditorial color selection for best browser compatibility without extra heavy libraries.
